apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pipeline-dev
  labels:
    app: pipeline
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: codefresh
    name: in-cluster
  project: pipeline
  source:
    repoURL: https://github.com/tpayne/codefresh-demo.git
    targetRevision: main
    path: ./helm/dev  # Directory that contains the child apps
    helm:
      parameters:
        - name: buildID
          value: argocd
      valueFiles:
        - values.yaml
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
#    automated:
#      prune: true